<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Maxblog</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="container">
    <header>
      <nav>
        {{#if loggedIn}}
        <button id="logoutButton" class="regularButton">logout</button>
        <a href="/dashboard">dashboard</a>
        {{else}}
        <a href="/login">login</a>
        {{/if}}
      </nav>
       <h1>
        <a href="/">Maxblog</a>
      </h1>
    </header>
    <main>
      {{{ body }}}
    </main>
  </div>
  
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>